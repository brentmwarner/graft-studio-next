# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Graft (for example via API key or ChatGPT auth supported by Codex).
- Graft starts the server via `codex app-server` per session.
