# iOS

Root `AGENTS.md` / `CLAUDE.md` still apply. Native SwiftUI — not Expo.

SwiftUI: Read `.agents/skills/swiftui-specialist/SKILL.md`. Mirror product behavior on `apps/mobile-android` in the same task.
