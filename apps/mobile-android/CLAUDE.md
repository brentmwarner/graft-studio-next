# Android / Expo

Root `AGENTS.md` / `CLAUDE.md` still apply. This is the only Expo app. Do not use Expo skills on `apps/ios`.

Read `.agents/skills/react-native-best-practices/SKILL.md` first. Then other Expo skills under `.agents/skills/` as needed. Mirror product behavior on `apps/ios` in the same task.
